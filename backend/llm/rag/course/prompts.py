"""[course] 직관 코스 추천 프롬프트 — 내용 규칙만. 말투는 ../persona 에서 가져온다.

핵심 원칙: LLM 은 후보 목록(P1, P2 …)에서 **키만 고르고** 짧은 이유와 두세 문장 인트로만 쓴다.
    · 장소 이름·좌표·주소 → 코드가 DB 값으로 붙인다 (좌표 환각 0)
    · 시각·도보 거리·코스 목록 → 코드가 계산해서 붙인다 (숫자 환각 0, 출력이 짧아져 응답도 빨라진다)
"""
from ..persona import PERSONA_HEADER, TONE_RULES

SYSTEM = f"""{PERSONA_HEADER}
지금은 "직관 코스"를 짜 주는 역할이다. 경기 전(BEFORE) → 경기 관람(GAME) → 경기 후(AFTER) 순서로
하루 코스를 만든다. 단, 너는 **후보 중에서 고르기만** 하고 목록·시각·거리는 프로그램이 만든다.

반드시 지킬 것
1. 장소는 아래 <candidates> 에 있는 것만 고른다. 목록에 없는 가게·명소를 지어내지 않는다.
2. 출력은 JSON 하나만. 설명이나 코드펜스 없이 JSON 만 쓴다. 형식:
   {{"course": [{{"place_key": "P3", "phase": "BEFORE", "reason": "든든한 한 끼, 구장까지 도보 8분"}},
               {{"place_key": "STADIUM", "phase": "GAME", "reason": "경기 관람"}},
               {{"place_key": "P11", "phase": "AFTER", "reason": "경기 끝나고 치맥"}}],
    "intro": "사용자에게 건네는 두세 문장 인트로"}}
3. BEFORE 1~2곳, GAME 은 항상 STADIUM 1곳, AFTER 1~2곳. 총 3~5곳.
   단 <situation> 에 "범위: 경기 전만" 이 있으면 AFTER 는 고르지 않고 BEFORE 를 1~3곳,
   "범위: 경기 후만" 이면 BEFORE 는 고르지 않고 AFTER 를 1~3곳 고른다. 사용자가 말하지 않은 구간을 덧붙이지 않는다.
4. BEFORE 는 식사 위주, AFTER 는 <after_hint> 에 적힌 종류 중에서 고른다.
   사용자가 경기 전에 카페·디저트도 가겠다고 하면 BEFORE 에 [CAFE] 후보를 넣어도 된다 (식사 → 카페 순서).
   <situation> 에 "이동: 자동차" 가 있으면 술집·주류 위주 장소는 어느 구간에도 고르지 않는다.
   [STAY] 는 숙소다. 사용자가 숙박을 원할 때만, AFTER 의 맨 마지막에 딱 1곳 넣는다 (그 뒤에 다른 장소를 두지 않는다).
   [WALK](산책)·[INDOOR](실내 놀거리)는 [SPOT] 처럼 경기 전이나 후에 넣을 수 있다. 추가 요청이 있으면 1곳 넣는다.
5. <situation> 의 취향·동행·여유는 반드시 반영한다. 특히 동행이 있으면 그에 맞는 후보를 우선한다.
6. 같은 프랜차이즈·같은 종류를 두 번 고르지 않는다.
7. 후보에 붙은 방위(북동쪽 900m 등)를 보고 **되도록 같은 방향으로 모아서** 고른다. 경기 전은 북쪽,
   경기 후는 남쪽처럼 흩어지면 사용자가 많이 걷는다.
8. reason 은 20자 안팎 한 문장. "도보 N분" 같은 숫자는 후보에 적힌 값만 쓴다.
9. intro 는 두세 문장. 경기 정보(날짜·시간·상대)를 한 줄로 먼저 말하고, 이 코스를 왜 이렇게 짰는지
   한 문장 덧붙인다. **장소 목록·시각·총 도보는 쓰지 마라** — 프로그램이 인트로 아래에 붙인다.
   영업시간·가격·예약 가능 여부는 자료에 없으니 말하지 않는다.
   주차·교통 정보도 프로그램이 인트로 아래에 붙이니 인트로에서 지어내지 않는다.

{TONE_RULES}"""

USER_TEMPLATE = """<game>
{game}
</game>

<situation>
{situation}
</situation>

<after_hint>
경기 후에는 {after_hint} 중에서 고른다.
</after_hint>

<candidates>
{candidates}
</candidates>

사용자 질문: {question}

위 규칙대로 JSON 만 출력해."""

WARN_THIRD_PARTY = "카카오맵 기준 정보라 가시기 전에 영업 여부만 한 번 확인해 보세요!"

# 경기가 없을 때 (날짜를 지정했는데 그날 홈경기가 없음)
NO_GAME = ("{date_ko}에는 {stadium_ko} 경기가 자료에 없어요. 가까운 홈경기는 이래요:\n{games}\n"
           "원하시는 날짜를 알려주시면 그날 기준으로 코스를 짜 드릴게요!")

# 후보가 아예 없을 때 (데이터 미적재 · 조건이 너무 좁음)
NO_PLACES = "{stadium_ko} 주변에서 조건에 맞는 곳을 못 찾았어요. 취향이나 구장을 다시 알려주시면 찾아볼게요."
